services:
  qdrant:
    image: qdrant/qdrant:v1.14.1  # pin version for safety
    ports:
      - "6333:6333"
    volumes:
      # Bind mount so data is on your host and easy to back up
      - ./qdrant_storage:/qdrant/storage
